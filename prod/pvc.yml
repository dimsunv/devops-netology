---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: static
  namespace: prod
spec:
  storageClassName: "nfs"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
